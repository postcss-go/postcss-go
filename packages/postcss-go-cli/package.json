{
  "name": "@postcss-go/cli",
  "version": "0.0.1",
  "description": "CLI for postcss-go, ported from postcss-cli",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "postcss-go": "./index.js"
  },
  "scripts": {
    "build": "node ./scripts/build-test-bridge.mjs",
    "check": "node -e \"console.log('postcss-go-cli: no typecheck step')\"",
    "pretest": "pnpm --filter @postcss-go/core build && node ./scripts/build-test-bridge.mjs",
    "test": "vitest run",
    "test:coverage": "pnpm --filter @postcss-go/core build && node ./scripts/build-test-bridge.mjs && vitest run --coverage.enabled true --coverage.provider v8 --coverage.reporter=text --coverage.reporter=lcov --coverage.reporter=json-summary"
  },
  "dependencies": {
    "@postcss-go/core": "workspace:*",
    "chokidar": "^3.6.0",
    "dependency-graph": "^1.0.0",
    "picocolors": "^1.1.1",
    "postcss-load-config": "^5.1.0",
    "postcss-reporter": "^7.1.0",
    "pretty-hrtime": "^1.0.3",
    "read-cache": "^1.0.0",
    "slash": "^5.1.0",
    "tinyglobby": "^0.2.15",
    "yargs": "^17.7.2"
  },
  "peerDependencies": {
    "postcss": "^8.0.0"
  },
  "devDependencies": {
    "postcss": "^8.5.15",
    "vitest": "^3.2.4"
  },
  "files": [
    "index.js",
    "lib",
    "dist",
    "!**/*.test.js"
  ],
  "keywords": [
    "cli",
    "postcss",
    "postcss-go",
    "postcss-runner"
  ],
  "license": "MIT"
}
