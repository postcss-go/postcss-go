# @postcss-go/wasm

Browser and worker-oriented wrapper package for `postcss-go`.

This package is intended to expose a higher-level initialization API for a future wasm runtime so playgrounds, editors, and browser-based tooling can use the Go engine.
