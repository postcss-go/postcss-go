name: CodSpeed

on:
  push:
    branches:
      - main
      - master
  pull_request:
  # `workflow_dispatch` allows CodSpeed to trigger backtest
  # performance analysis in order to generate initial data.
  workflow_dispatch:

concurrency:
  group: codspeed-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write # required for OIDC authentication with CodSpeed

jobs:
  go-benchmarks:
    name: Go Benchmarks
    # Go CodSpeed currently supports walltime only. Personal GitHub accounts
    # cannot use `codspeed-macro`; switch to that runner once the repo is under
    # an organization with CodSpeed Macro Runners enabled. Until then, walltime
    # on ubuntu-latest is noisy ("Different runtime environments detected") and
    # ~10% swings without Go engine changes should be treated as CI noise.
    runs-on: ubuntu-latest
    env:
      GOFLAGS: -mod=mod

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run Go benchmarks
        uses: CodSpeedHQ/action@v4
        with:
          mode: walltime # go only supports walltime mode
          token: ${{ secrets.CODSPEED_TOKEN }}
          # CodSpeed Go runner only accepts the `-bench` flag.
          # Exclude *_Small / *_small: on ubuntu-latest walltime those finish in
          # tens of µs and routinely trip the ~10% regression threshold with no
          # Go engine change (see CodSpeed "Different runtime environments").
          # Local `go test -bench=. ./benchmark/` still runs the full suite.
          run: >-
            go test ./benchmark/
            -bench=Benchmark(Parse|ParseStringify|Process).*(_Medium|_Large|Real_)|Benchmark(Tokenize|Walk|Plugin|Sourcemap)_(medium|large|bootstrap|tailwind)
